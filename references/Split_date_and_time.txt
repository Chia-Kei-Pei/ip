- rename this directory from "agents" to "references" #DONE
- Split date and time in Task classes.
- Use TodoCommand, DeadlineCommand, and EventCommand to parse from storage file, since they can take purely positional arguments